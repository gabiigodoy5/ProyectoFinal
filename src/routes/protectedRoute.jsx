import { Route } from "react-router-dom"


const ProtectedRoute = () => {
    return(
        <Route 
        render={() =>{
            if()
        }
        }

        />
    );
};

/*codigo de validación para la página a la q quiero <acceder acceder/*